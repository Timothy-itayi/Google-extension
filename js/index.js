// Create the HTML elements
const h1 = document.createElement('h1')

// Set the content for the h1 element
h1.textContent = 'Hello World'

// Add the h1 element to the document body
document.body.appendChild(h1)
